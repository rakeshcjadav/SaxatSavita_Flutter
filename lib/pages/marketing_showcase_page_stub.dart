import 'package:flutter/material.dart';

// Stub implementation for release builds
class MarketingShowcasePage extends StatelessWidget {
  const MarketingShowcasePage({super.key});

  @override
  Widget build(BuildContext context) {
    // This should never be called in release builds
    return const SizedBox.shrink();
  }
}
